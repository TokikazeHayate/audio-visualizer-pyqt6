# setup.py
from setuptools import setup

setup(
    name="audio-visualizer",
    version="1.0",
    packages=[""],
    install_requires=[
        "PyQt6",
        "numpy",
        "pyaudio",
        "pyinstaller"
    ]
)